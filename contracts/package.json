{
  "name": "my-contracts",
  "version": "1.0.0",
  "description": "My contracts",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat ignition deploy ./ignition/scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.9",
    "@nomiclabs/hardhat-etherscan": "^3.1.8",
    "hardhat": "^2.22.9"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}

